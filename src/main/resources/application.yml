spring:
  application:
    name: byte-history-api
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
  jackson:
    default-property-inclusion: non_null
  cache:
    type: simple
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

server:
  port: 8080